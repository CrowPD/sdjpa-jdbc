spring.application.name=sdjpa-jdbc

spring.datasource.username=bookadmin
spring.datasource.password=adminpass
spring.datasource.url=jdbc:mysql://localhost:3308/bookdb2?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC
spring.jpa.hibernate.ddl-auto=validate

spring.sql.init.mode=always

spring.flyway.user=bookadmin
spring.flyway.password=adminpass

# Enable SQL logging
#spring.jpa.show-sql=true
spring.jpa.properties.hibernate.show_sql=true

# Enable SQL formatting for Hibernate *specifically*
spring.jpa.properties.hibernate.format_sql=true

# Enable tracing values being inserted
logging.level.org.hibernate.orm.jdbc.bind=TRACE